{
  "name": "terraform-provider-searchstax",
  "version": "0.0.0-semantically-released",
  "description": "Automated versioning and release tooling for the SearchStax Terraform provider.",
  "private": true,
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^24.2.3"
  }
}
